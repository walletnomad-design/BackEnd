# Connection string de PostgreSQL (Railway). Copia este archivo a .env y completa el valor real.
# EL arquivo .env NUNCA se sube al repositorio.
DATABASE_URL=
JWT_SECRET=
GEMINI_API_KEY=

AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SES_FROM_EMAIL=
